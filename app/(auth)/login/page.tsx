import AuthClient from "@/components/auth/AuthClient";

const LoginPage = () => {
  return (
    <section className="justify-center flex items-center h-full ">
      <AuthClient />
    </section>
  );
};

export default LoginPage;
